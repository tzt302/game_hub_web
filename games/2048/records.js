export const SCORE_KEY="tzt-2048-scores-v1";
export function normalizeScores(value){if(!Array.isArray(value))return[];const byId=new Map();value.filter(item=>item&&Number(item.score)>0).forEach(item=>{const record={id:String(item.id||item.achievedAt||"legacy"),score:Math.round(Number(item.score)),maxTile:Math.max(2,Math.round(Number(item.maxTile)||2)),achievedAt:String(item.achievedAt||"")};const current=byId.get(record.id);if(!current||record.score>current.score)byId.set(record.id,record)});return[...byId.values()].sort((a,b)=>b.score-a.score||b.maxTile-a.maxTile).slice(0,10);}
export function recordScore(records,entry){const current=normalizeScores(records),previous=current.find(item=>item.id===String(entry.id));if(previous&&previous.score>=Number(entry.score))return current;return normalizeScores([...current.filter(item=>item.id!==String(entry.id)),entry]);}
export function loadScores(storage=globalThis.localStorage){try{return normalizeScores(JSON.parse(storage?.getItem(SCORE_KEY)||"[]"))}catch{return[]}}
export function saveScores(records,storage=globalThis.localStorage){const normalized=normalizeScores(records);storage?.setItem(SCORE_KEY,JSON.stringify(normalized));return normalized;}
