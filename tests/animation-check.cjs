const { chromium } = require('playwright');
const assert = require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,executablePath:'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe'});const page=await browser.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true});const errors=[];page.on('pageerror',error=>errors.push(error.message));await page.goto('http://127.0.0.1:8766/games/2048/');let sawMotion=false;for(const key of ['ArrowLeft','ArrowRight','ArrowUp','ArrowDown']){await page.keyboard.press(key);await page.waitForTimeout(40);if(await page.locator('.motion-layer .tile-ghost').count()){sawMotion=true;assert.equal(await page.locator('.tiles-hidden').count(),1);break}await page.waitForTimeout(230)}assert.equal(sawMotion,true);await page.waitForTimeout(260);assert.equal(await page.locator('.motion-layer').count(),0);assert.deepEqual(errors,[]);console.log('2048 animation layer and cleanup passed');await browser.close()})().catch(error=>{console.error(error);process.exit(1)});
