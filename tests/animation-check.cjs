const { chromium } = require('playwright');
const assert = require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,executablePath:'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe'});const page=await browser.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true});const errors=[];page.on('pageerror',error=>errors.push(error.message));await page.goto('http://127.0.0.1:8766/games/2048/');let sawMotion=false;for(const key of ['ArrowLeft','ArrowRight','ArrowUp','ArrowDown']){const before=await page.locator('.tiles .tile').count();await page.keyboard.press(key);await page.waitForTimeout(40);if(await page.locator('.tile-moving').count()){sawMotion=true;assert.equal(await page.locator('.tiles-hidden').count(),0);assert.equal(await page.locator('.tiles .tile').count(),before);break}await page.waitForTimeout(220)}assert.equal(sawMotion,true);await page.waitForTimeout(240);assert.equal(await page.locator('.tile-moving').count(),0);assert.deepEqual(errors,[]);console.log('2048 moves existing tiles without hiding the text layer');await browser.close()})().catch(error=>{console.error(error);process.exit(1)});
